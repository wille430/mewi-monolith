<!DOCTYPE html>
hmtl(lang="en")
    head
        title
    body
        header
            figure
                img(src=logoUrl class="logo")
        section.content
            block content

        footer

        style.
            body {
                padding: 0;
                margin: 0;
                top: 0;
                font-family: Arial, Helvetica, sans-serif;
            }

            footer {
                min-height: 200px;
            }

            header, footer {
                background: #253C4C;
                display: flex;
            }

            .logo {
                width: 100px;
                height: auto;
            }

            .content {
                padding: 16px 24px;
                min-height: 200px;
            }